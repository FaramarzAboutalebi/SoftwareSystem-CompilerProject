#Aouthors' names:

- Faramarz Aboutalebi
- Ethan Stein

# Running The Program

- In order to run this program GCC Compiler must be installed.

- Whilst in the same directory as `hwk4compiler.c` run the 
  following command in order to compile: 
  
            gcc -o hwk4compiler hwk4compiler.c

- Whilst in the same directory as `hwk4compiler.exe` run the
  following command in order to execute: 
            ./hwk4compiler {YOUR INPUT FILE NAME}.txt

- After execution, executable linkable file, should exist in 
  current directory as `elf.txt`. 
